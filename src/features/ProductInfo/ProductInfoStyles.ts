import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  reviewContainer: {
    flex: 1,
    alignItems: 'center',
    marginBottom: 20,
    padding: 15,
    borderRadius: 8,
    borderColor: '#eee',
    borderWidth: 1,
  }
});
